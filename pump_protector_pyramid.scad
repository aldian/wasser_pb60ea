// ============================================
// Wasser PB-60EA Booster Pump Protector — PYRAMID
// Print upside down (apex on build plate).
// No supports needed.
// Size: ~197 x 157 x 20 mm
// ============================================

include <pump_protector.scad>

protector_pyramid();
